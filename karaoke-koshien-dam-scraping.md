# カラオケ甲子園 DAMランキング スクレイピング仕様

## 1. 方針

DAMの公開ランキングページを事前に（バッチで）スクレイピングし、`songs` テーブルに保存しておく。アプリ本体からのリアルタイムスクレイピングは行わない（レスポンス速度・DAM側への負荷を考慮）。

## 2. 実行方式

- Node.jsのスクリプトとして実装し、Render上でCron Job（無料枠にCronはないため、無料で回すなら GitHub Actions の scheduled workflow から実行し、結果をNeonに直接書き込む方式を推奨）
- 取得はcheerio等の軽量HTMLパーサーを使用。DAM側の構造次第でPuppeteer等のヘッドレスブラウザが必要になる可能性もあるため、まず対象ページのHTML構造を確認してから実装方式を確定する
- 取得頻度は週1回程度を想定（ランキング自体の更新頻度に合わせる）

## 3. データ保存

- `songs` テーブルに `title`, `artist`, `dam_rank`, `scraped_at` を保存
- 新しく取得したデータは既存データを上書きせず追加保存し、`scraped_at` が最新のレコード群のみを選曲候補として扱う（スクレイピング失敗時に古いが正常なデータへフォールバックできるようにするため）
- 極端に古いデータ（例：1ヶ月以上前）だけが残っている場合は、選曲画面に「ランキングが更新されていません」的な表示を出す運用も検討可能（優先度は低）

## 4. 選曲ロジックとの連携

- 選曲時は `songs` テーブルの最新 `scraped_at` のレコード群からランダムに1曲を返す
- 同一ラウンド内で複数のperformanceがある場合、曲がかぶらないよう `GET /api/songs/random?exclude=...` で既に提案済みの曲を除外する

## 5. 留意点

- スクレイピング対象・頻度はDAM側の利用規約・robots.txtの範囲内で行う（個人利用の範囲を超えた高頻度アクセスは避ける）
- HTML構造の変更に弱いため、取得件数が0件だった場合はエラーとして検知し、前回成功分のデータをそのまま使い続けるようにする（3章の設計と連動）
