# カラオケ甲子園 API・エッジケース設計

## 1. REST APIエンドポイント

リアルタイム操作はSocket.ioで行うが、ルーム作成・参加ページの初期表示・曲データ取得など「1回きりの読み書き」はREST APIで済ませる。

| メソッド | パス | 用途 | レスポンス例 |
|---|---|---|---|
| POST | `/api/sessions` | ルーム作成 | `{ sessionId, roomCode, hostToken }` |
| GET | `/api/sessions/:roomCode` | 参加ページ表示用にルーム存在確認 | `{ exists: true, status: "waiting" }` |
| POST | `/api/sessions/:roomCode/participants` | 参加者登録（参加後はSocket.ioに切り替え） | `{ participantId }` |
| GET | `/api/songs/random` | DAMランキングからランダムに1曲取得 | `{ title, artist }` |
| GET | `/api/songs/random?exclude=uuid1,uuid2` | 同ラウンド内で曲がかぶらないよう除外指定 | `{ title, artist }` |

参加者登録後の状態同期・得点入力・順位更新などはすべてSocket.ioイベント（別途 `karaoke-koshien-socketio-events.md` を参照）で行う。

## 2. エッジケースの扱い

### 参加人数が少ない場合

- **0人／1人でラウンド開始しようとした場合**：`round:start` を拒否し、`error` イベントで `NOT_ENOUGH_PARTICIPANTS` を返す。個人戦は最低2人、チーム戦は最低2人（うち1人はソロ扱いになりうる）から開始可能とする
- **チーム戦の選択肢自体を出さない**：有効な参加者が1人以下の場合、ランダム提案の候補から「チーム戦」を除外し、個人戦のみ提案する

### 同名参加者

- 参加時にサーバー側で名前の重複をチェックし、重複があれば自動的に「名前(2)」のように連番を付与して登録する（参加をブロックしない）

### 得点入力ミス

- そのラウンドが完全に確定する（＝次のラウンドが `round:start` される）までは、`score:correct` イベントで何度でも修正可能とする
- 次のラウンドが始まった後は編集不可（ロック）とし、誤操作で過去の結果が変わってしまう事態を防ぐ

### 回線切断・再接続

- Socket.io切断を検知したクライアントは、再接続後に自動で `session:sync_request` を送り、`session:state_full` で最新状態を丸ごと復元する
- ホスト端末が切断・リロードされた場合も、`localStorage` に保存済みの `hostToken` を使って再接続時にホスト権限を復元する

### ルーム関連

- **ルームコードの桁数**：6桁の英数字（例：`AB12CD`）とし、紛らわしい文字（`0`/`O`、`1`/`I` など）は生成時に除外する
- **有効期限**：ルーム作成から24時間で自動失効とする（無料DB枠を圧迫しないよう、期限切れセッションは定期的にクリーンアップするバッチも用意する）
- **同じルームコードへの二重参加**：同一 `participantId` を持つ端末が再度 `participant:join` した場合は新規登録せず、既存の参加者情報をそのまま返す（再接続と同じ扱い）

### DAMランキングが取得できない場合

- スクレイピングが失敗した場合は、DB内にある直近の正常取得データをそのまま使い続ける（`songs` テーブルは上書きせず、`scraped_at` が最新の成功分を参照する設計にする）
